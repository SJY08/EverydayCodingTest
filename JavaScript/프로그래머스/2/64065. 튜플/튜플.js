const solution = s => s.slice(1, -1).split("}").map(v => v.startsWith("{") ? v.slice(1) : v.startsWith(",{") ? v.slice(2) : null).filter(Boolean).map(v => v.split(",").map(Number)).sort((a,b)=>a.length-b.length).reduce((a,v)=>[...new Set([...a,...v])],[])